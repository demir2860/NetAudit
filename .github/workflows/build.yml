name: Build NetAudit Pro Wireshark

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '6.0.x'

    - name: Restore
      run: dotnet restore NetAudit.UI/NetAudit.UI.csproj

    - name: Build
      run: dotnet build NetAudit.UI/NetAudit.UI.csproj -c Release

    - name: Publish
      run: dotnet publish NetAudit.UI/NetAudit.UI.csproj -c Release -o ./dist --self-contained

    - name: Rename EXE
      shell: pwsh
      run: |
        if (Test-Path dist/NetAudit.UI.exe) {
          Rename-Item -Path dist/NetAudit.UI.exe -NewName NetAuditWireshark.exe
        }

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: NetAuditWireshark
        path: dist/
        retention-days: 90
